import React from 'react'
import { motion } from 'framer-motion'

function ScrollDotsNav({lenght,curr}) {
  return (
    <motion.section>
        ScrollDotsNav
    </motion.section>
  )
}

export default ScrollDotsNav